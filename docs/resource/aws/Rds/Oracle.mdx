---
title: Oracle
sidebar_position: 0
tags: [AWS, 리소스, Rds, Oracle]
---

import icon from "../img/rds-oracle.png"

<span className="theme-doc-version-badge badge badge--secondary">
    last update: 2023.05.01
</span>

##### 읽는 데 3분 걸림(소요시간)

---

## 리소스 이미지

<img src={icon} />

## 리소스 개요

Amazon RDS for Oracle은 클라우드에서 Oracle 배포를 손쉽게 설정, 운영 및 확장할 수 있는 완전관리형 상용 데이터베이스입니다. Amazon RDS는 프로비저닝, 백업, 소프트웨어 패치, 모니터링, 하드웨어 크기 조정 같은 시간 소모적인 데이터베이스 관리 업무를 대신 처리하므로 고객은 혁신과 애플리케이션 개발에만 집중할 수 있습니다.

## 연관 리소스

### 상위 리소스

-   - [**Subnet**](https://docs.cstudio.app/docs/resource/azure/Network/Subnet)

### 연결 리소스

-   **Oracle**

## 리소스 설정 값

-   `engine` : RDS Instance의 Oracle Engine 종류 - `oracle-ee`, `oracle-se2`
-   `engine_version` : RDS에 사용할 Oracle 버전
-   `instance_class` : RDS DB의 가격 측정 계층
-   `storage_type` : RDS 인스턴스의 Storage Type
-   `allocated_storage` : RDS Insatance Storage의 용량 (Gigabytes)
-   `max_allocated_storage` : RDS에
-   `username` : RDS의 사용자 이름
-   `password` : RDS의 사용자 패스워드
-   `skip_final_snapshot` : DB 인스턴스가 삭제되기 전에 최종 DB 스냅샷을 생성할지 여부를 결정 - `true`, `false`
-   `included_subnet_names` : RDS Subnet 그룹에 연결할 Subnet 이름
-   `backup_retention_period` : 백업 데이터를 보관할 기간
-   `storage_encrypted` : DB 인스턴스의 암호화 여부를 지정 - `true`, `false`
-   `publicly_accessible` : 인스턴스에 public access가 가능한지 여부 - `true`, `false`

---

## 참고 자료

-   [Amazon RDS for Oracle 기능(Amazon RDS)(AWS)](https://aws.amazon.com/ko/rds/oracle/features/?nc=sn&loc=2&dn=1)
-   [Amazon RDS for Oracle 요금(AWS)](https://aws.amazon.com/ko/rds/oracle/pricing/)
