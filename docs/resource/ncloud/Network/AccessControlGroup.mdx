---
title: Access Control Group
sidebar_position: 0
tags: [Ncloud, 리소스, Network, AccessControlGroup]
---

import icon from "../img/network-access_control_group.png"





<hr />
<br />

## 리소스 아이콘

<img src={icon} />

## 리소스 개요

서버 간 네트워크 접근 제어 및 관리를 할 수 있는 IP/Port 기반 필터링 방화벽 서비스입니다. ACG는 개별 방화벽을 구축하지 않고도 ACG에 서버 그룹별 방화벽 규칙을 설정하여 인프라 보안 정책을 손쉽게 적용할 수 있습니다.

## 연관 리소스

### 상위 리소스

-   [**Vpc**](https://docs.cstudio.app/docs/resource/ncloud/Network/Vpc)

## 리소스 설정 값

-   `included_vpc_name` : ACG를 생성할 VPC 이름
-   `acg_description` : ACG에 대한 설명
-   `inbound_rule`
    -   `protocol` : 인바운드 트래픽의 프로토콜 - `TCP`, `UDP`, `ICMP`
    -   `ip_block` : 인바운드 트래픽 접근 소스, CDIR 형식의 IP 대역
    -   `port_range` : 인바운드 규칙을 적용할 포트 범위, 1 ~ 65535의 범위
-   `outbound_rule` :
    -   `protocol` : 아웃바운드 트래픽의 프로토콜 - `TCP`, `UDP`, `ICMP`
    -   `ip_block` : 아웃바운드 트래픽 목적지, CDIR 형식의 IP 대역
    -   `port_range` : 아웃바운드 규칙을 적용할 포트 범위, 1 ~ 65535의 범위(범위 설정시: 100-200)

---

## 참고 자료

-   [ACG(Ncloud)](https://www.ncloud.com/product/security/acg)
-   [ncloud_access_control_group(Terraform)](https://registry.terraform.io/providers/NaverCloudPlatform/ncloud/latest/docs/resources/access_control_group)
