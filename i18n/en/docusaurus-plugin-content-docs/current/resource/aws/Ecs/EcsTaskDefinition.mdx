---
title: EcsTaskDefinition
sidebar_position: 0
tags: [AWS, Resource, Ecs, EcsTaskDefinition]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblob.blob.core.windows.net/cloudstudio/awsImg/ecs-ecs_task_definition.png"
    }
/>

## Resource Overview


## Related Resources

### Parent Resources

-   [**Region**](https://docs.cstudio.app/docs/resource/aws/Base/Region)


## Resource Configuration Values

-   `app_environment`: Launch type required for the task - `EC2`, `Fargate`
-   `os_architecture`: Operating system family to use when registering the task definition
-   `task_cpu`: Number of CPU units used by the task
-   `task_memory`: Amount of memory used by the task (MiB)
-   `network_mode`: Networking mode to use for the task's container - `awsvpc`, `none`, `bridge`, `host`
-   `container`: Name of the container to run in the ECS Cluster
-   `container.image_url`: Image of the container to run in the ECS Cluster
-   `container.essential_enabled`: Whether other objects related to the container should stop or continue running if the container stops - `false`, `true`
-   `container.port_mapping`: Port mapping details
  -     `container.port_mapping.name`: Name of the port mapping
  -     `container.port_mapping.container_port`: Port number used by the container for traffic to and from the host container instance
  -     `container.port_mapping.protocol`: Protocol used for port mapping - `tcp`, `udp`
  -     `container.port_mapping.app_protocol`: Application protocol used for service connections - `http`, `http2`, `grpc`, `none`
-   `container.cpu`: CPU capacity of the container running in the ECS Cluster
-   `container.memory`: Memory capacity of the container running in the ECS Cluster

-   `volume.name`: Name of the volume to attach to the task definition
-   `volume_mount.name`: Name of the volume to attach to the task definition
-   `volume_mount.container_name`: Name of the container where the volume will be mounted
-   `volume_mount.volume_name`: Name of the volume to be mounted
-   `volume_mount.container_path`: Path in the container where the volume will be mounted
-   `volume_mount.read_only`: Whether the container has read-only access to the volume - `false`, `true`


## Reference Materials

-   [Amazon Ecs Task Definition(AWS)](https://docs.aws.amazon.com/ko_kr/AmazonECS/latest/developerguide/task_definitions.html)
-   [Amazon Ecs Task Definition(Terraform)](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/ecs_task_definition)
