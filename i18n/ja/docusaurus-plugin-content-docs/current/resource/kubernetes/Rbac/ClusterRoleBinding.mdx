---
title: ClusterRoleBinding
sidebar_position: 0
tags: [Kubernetes, リソース, RBAC, ClusterRoleBinding]
---

<hr />
<br />

## リソースアイコン

<img
    src={
        "https://cloudstudioblob.blob.core.windows.net/cloudstudio/kubernetesImg/rbac-cluster_role_binding.png"
    }
/>

## リソース概要

クラスター レベルとすべてのネームスペースで適用される役割をユーザーに参照するクバネティス リソース

## 関連リソース

### 上位リソース

-   [**Cluster**](https://docs.cstudio.app/ja/docs/resource/kubernetes/Base/Cluster)

### 接続リソース

-   [**ClusterRole**](https://docs.cstudio.app/ja/docs/resource/kubernetes/Rbac/ClusterRole)

## リソース設定値

-   `service_name` : ClusterRoleBinding リソース名
-   `annotations` : クバネティスシステムで必要な情報を表示するために使用する key-value
-   `labels` : オブジェクトを区切るためにユーザーが任意に希望する値を指定
-   `role_name` : 主体をバインディングする Cluster Role の名前を指
-   `subject` : Cluster Role を付与する主体を指定
    -   `name` : バインディングする主体の名前を入力
    -   `namespace` : バインディングする主体のネームスペースを入力
    -   `api_group` : 承認決定を下す API Group - `rbac.authorization.k8s.io`,`User`,`Group`
    -   `kind` : バインディングする主体のタイプを選択 - `ServiceAccount`,`User`,`Group`

---

## 参考資料

-   [ClusterRoleBinding(kubernetes)](https://kubernetes.io/docs/reference/access-authn-authz/rbac/#rolebinding-and-clusterrolebinding)
-   [ClusterRoleBinding(Terraform)](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/cluster_role_binding)
    `
