---
title: Azure
sidebar_position: 1
tags: [security, セキュリティ診断, キャンバス]
---

import styles from "/src/scss/_markdown.module.scss"
import { azureRules } from "../resource/azureRules"
import { iso, csap, isms, evaluation } from "../resource/rules"

<hr />
<br />

## Azure 診断項目

<div className={styles.securityTable}>
    <table>
        <thead>
            <tr>
                <th>カテゴリ</th>
                <th>リソース</th>
                <th>オプション</th>
                <th>項目</th>
                <th>説明</th>
                <th>危険度</th>
            </tr>
        </thead>
        <tbody>
            {azureRules.map((item)=>{
                return (<tr>
                            <td>{item.category}</td>
                            <td>{item.resource}</td>
                            <td>{item.option}</td>
                            <td>{item.type}</td>
                            <td>{item.description}</td>
                            <td><div className={styles.tag} data-type={item.risk}>{item.risk.toUpperCase()}</div></td>
                        </tr>)
            })}
        </tbody>
    </table>

</div>
